<Window x:Class="AI.KB.Assistant.Views.LauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI.KB.Assistant - 啟動器"
        Height="520" Width="820"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource App.BackgroundBrush}"
        Foreground="{DynamicResource App.TextBrush}"
        ResizeMode="NoResize">
    <Window.Resources>
        <!-- 共用按鈕樣式（沿用 Main 風格） -->
        <Style x:Key="TonedBtn" TargetType="Button">
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="Background" Value="{DynamicResource App.PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource App.BorderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource App.TextBrush}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!-- 大卡片 -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="{DynamicResource App.PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource App.BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- 標頭 -->
        <Border DockPanel.Dock="Top" Padding="16"
                Background="{DynamicResource App.PanelBrush}"
                BorderBrush="{DynamicResource App.BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="AI.KB.Assistant" FontSize="22" FontWeight="Bold"/>
                <TextBlock Text="  —  簡易啟動介面" Margin="8,0,0,0"
                           Foreground="{DynamicResource App.TextMutedBrush}"/>
            </StackPanel>
        </Border>

        <!-- 主體 -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 四顆按鈕 -->
            <UniformGrid Grid.Row="0" Rows="2" Columns="2" Margin="10">
                <Border Style="{StaticResource Card}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="⚡ 自動分類" FontSize="24" FontWeight="Bold" />
                        <TextBlock Text="掃描收件夾，建立清單並預分類" Margin="0,6,0,12"
                                   Foreground="{DynamicResource App.TextMutedBrush}"/>
                        <Button Content="開始" Style="{StaticResource TonedBtn}" Click="BtnAutoClassify_Click"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="🗂 收件夾" FontSize="24" FontWeight="Bold" />
                        <TextBlock Text="開啟 HotFolder（收件匣）" Margin="0,6,0,12"
                                   Foreground="{DynamicResource App.TextMutedBrush}"/>
                        <Button Content="開啟" Style="{StaticResource TonedBtn}" Click="BtnOpenInbox_Click"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="🖼 詳細版" FontSize="24" FontWeight="Bold" />
                        <TextBlock Text="進入完整操作介面（樹/清單/AI 面板）" Margin="0,6,0,12"
                                   Foreground="{DynamicResource App.TextMutedBrush}"/>
                        <Button Content="進入" Style="{StaticResource TonedBtn}" Click="BtnOpenDetailed_Click"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="⚙ 設定" FontSize="24" FontWeight="Bold" />
                        <TextBlock Text="路徑 / 黑名單 / 門檻 / 啟動模式" Margin="0,6,0,12"
                                   Foreground="{DynamicResource App.TextMutedBrush}"/>
                        <Button Content="開啟設定" Style="{StaticResource TonedBtn}" Click="BtnOpenSettings_Click"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- 底部路徑摘要 -->
            <Border Grid.Row="1" Padding="10"
                    Background="{DynamicResource App.PanelBrush}"
                    BorderBrush="{DynamicResource App.BorderBrush}" BorderThickness="1,1,1,0">
                <StackPanel>
                    <TextBlock Text="目前環境：" FontWeight="Bold"/>
                    <WrapPanel Margin="0,6,0,0">
                        <TextBlock Text="ROOT：" Foreground="{DynamicResource App.TextMutedBrush}"/>
                        <TextBlock x:Name="TxtRoot" Margin="6,0,18,0"/>
                        <TextBlock Text="INBOX：" Foreground="{DynamicResource App.TextMutedBrush}"/>
                        <TextBlock x:Name="TxtInbox" Margin="6,0,18,0"/>
                        <TextBlock Text="DESKTOP：" Foreground="{DynamicResource App.TextMutedBrush}"/>
                        <TextBlock x:Name="TxtDesktop" Margin="6,0,18,0"/>
                        <TextBlock Text="DB：" Foreground="{DynamicResource App.TextMutedBrush}"/>
                        <TextBlock x:Name="TxtDb" Margin="6,0,0,0"/>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
